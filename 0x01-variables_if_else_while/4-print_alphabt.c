#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description:alphabet in lowercase except q and e
 *
 * Return:  0 (Success)
 */

int main(void)
{

	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		if (alpha != 'q' && 'e');
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
