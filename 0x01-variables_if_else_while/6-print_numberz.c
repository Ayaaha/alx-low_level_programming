#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description:alphabet in low prints all single digit numbers
 *			of base 10 starting from 0
 *			using putchar function
 *
 * Return:  0 (Success)
 */

int main(void)
{

	int numz;

	for (numz = 0; numz <= 9; numz++)
	{
		putchar(int numz);
	}
		putchar('\n');
	return (0);
}
