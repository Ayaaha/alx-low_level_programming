#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description:alphabet in lowercase
 *
 * Return Always 0 (Success)
 */
int main(void)
{

	char alpha = 'a';
	int i;

	for (i = 1, i < 26, i++)
		int putchar(alpha);
	return (0);
}
