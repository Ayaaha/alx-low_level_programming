#include "main.h"

/**
 * _atoi -  convert a string to an integer.
 * @s: string to be converted to integer
 * Return: 1 Success
 */

int _atoi(char *s)
{
	/*
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	
	}*/
	int num;

	num = atoi(s);
	return (num);
}
