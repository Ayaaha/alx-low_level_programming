#include "main.h"
#include <unistd.h>
#include <stdio.h>

/**
 *_putchar - write the character in the output
 * @ch: charater that will be printed
 * Return: 1 on (success)
 */

int _putchar (char ch)
{
return (putchar(ch));
}
